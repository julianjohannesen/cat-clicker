// I have an array of photos
// I don't know how photos are going to be in the array
// I can look at the array's length after it loads
// I have a rough indication of the photo sizes in a size property with value being one of: pnt, fpm, x, pn, t
// Each photo contains a width embedded in its URL